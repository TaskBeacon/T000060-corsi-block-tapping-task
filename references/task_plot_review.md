# Task Plot Review

## Attempt 1

Evidence match: rejected.

- Every board snapshot shows eight blocks instead of the required nine.
- Forward recall arrows follow the encoded order and backward arrows reverse it correctly.
- Phase order, blue recall marker, practice-only feedback, and blank phase are correct.
- Encode timing labels are ambiguous: the second and third mini-panels are labeled as gaps rather than showing a consistent 500 ms highlight duration with a shared 500 ms gap annotation.

Targeted revision:

1. Use exactly nine outlines in a fixed 3-by-3 spatial arrangement in every board and mini-board snapshot.
2. Label each of the three encoding highlights `500 ms`; place one shared `500 ms gap` note beneath or between the mini-panels.

## Attempt 2

Evidence match: rejected.

- Encode timing labels are now correct and readable.
- Forward and backward recall arrows correctly show same and reverse order.
- Board snapshots still contain eight rather than nine block outlines.
- Feedback screens unnecessarily retain board outlines, increasing visual counting errors.

Final targeted revision:

1. Construct each board from nine explicit spatial slots: three upper, three middle, three lower; place one outline in every slot, then offset the slots slightly to preserve an irregular Corsi arrangement.
2. Use plain black feedback screens with only a check or cross, so block-count constraints apply only to Ready, Encode, and Recall boards.

## Final Review

Accepted after a minimal local image-model edit removed unreliable numeric glyphs from the Recall click markers while preserving arrow direction.

- Every Ready, Encode, and Recall board contains exactly nine blocks in the same arrangement.
- Forward arrows travel from the first encoded location through the second to the third; Backward arrows travel from the third through the second to the first.
- Phase order and timing labels match the evidence brief.
- The blue ready marker appears in Recall, and feedback is explicitly marked `Practice only`.
- Text is legible with no overlap or garbling.
- The final fixed header contains the centered title, centered `Construct:` subtitle, and borderless top-right TaskBeacon lockup.
- README embeds `![Task Flow](task_flow.png)` immediately under `## 2. Task Flow`.
